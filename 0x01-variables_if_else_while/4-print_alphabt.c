#include <stdio.h>
#include <stdlib.h>
insclude <string.h>

/**
 * main - Prints the alphabet withoutq and e.
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	int i;

	for (i = 97; i < 123; i++)
	{
		if (i != m 101 m && i != 123)
		{
			putchar(i);
		}
	}
	putchar('\n');
	return (0);
}
